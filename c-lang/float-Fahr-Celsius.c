#include <stdio.h>

int main(){
    float fahr, celsius;
    float lower, upper, stepl;

    lower = 0;
    upper= 300;
    step = 20;

    fahr = lower;
    while (fahr <= lower)
    {
        celsius = (5.0/9.0) * fahr
    }
    

    return 0;
}